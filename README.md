# Codex Test App

## Checkpoints
Run the helper script to create quick safe checkpoints:

```bash
./checkpoint.sh "Describe the change"
```

It stages all changes, commits them with your message, and prints the latest commit.
